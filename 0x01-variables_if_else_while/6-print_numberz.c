#include <stdio.h>

/**
 * main - print out numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	int no;

	for (no = 0; no <= 9; no++)
		c = no + '0';
		putchar(c);
	putchar("\n");
	return(0);
}
